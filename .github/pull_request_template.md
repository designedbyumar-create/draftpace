## What this changes

<!-- One or two sentences. What is different for a person using Draftpace? -->

## Why

<!-- The problem, not the patch. If it fixes something, say what was broken
     and how somebody would have noticed. -->

## How it was verified

<!-- Beyond "the gates pass". If there is a new guard test, say what happens
     when you break the code it guards: a test that has never been seen to
     fail is not yet known to work. -->

- [ ] `npx tsc --noEmit`
- [ ] `npx eslint .`
- [ ] `npm run test`
- [ ] `npm run build`
- [ ] Checked on a phone, if it changes anything visual

## Anything a reviewer should know

<!-- Trade-offs taken, things deliberately left out, anything that needs a
     matching change outside this repo (a Lemon Squeezy price, a Supabase
     migration, a Vercel environment variable). -->
